# GoKnowledgePoint

非书籍内获得的知识点

- 什么是原子（atomic）操作？
  指令顺序在执行中不会被干扰的操作。并发场景中，并发读写往往是不安全的，可能存在`竞争条件`，这些情况下会用到锁。但是对于一些操作，GO语言提供了`sync/atomic`标准库，其中的所有函数都是原子性的，在读写过程中不会受到干扰,可以不用锁。（一些原子操作的实现是对应一条机器指令的，还有一些实现更复杂些，不多讨论）
